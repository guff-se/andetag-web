# Phase 9 execution checklist, maintenance program (placeholder)

Purpose: track the transition from **migration** (phased rebuild, URL parity, locale rollout) to **maintenance** (ongoing updates on live **`www`**). Normative phase summary: **`docs/grand-plan.md`** (Phase 9).

**Status:** **Phase 8 closed** **2026-04-14** (**`docs/phase-8-verification-record.md`** §Closure). **Active carries from Phase 8:** **P9-25** (post-cutover release discipline), **P9-26** (post-cutover organic monitoring, former **P8-26**). The **Agent Skills**, **production PR gate**, and **P9-00** governance rows remain **TBD** until Gustaf scopes them.

## Governance and scope (TBD)

- [ ] **P9-00** Document maintenance scope: what lives in this repo vs external tools; who approves content, **`de`** copy, and analytics changes.
- [ ] **P9-25** **Post-cutover release discipline** (carried from Phase 8 **P8-25** **2026-04-14**): Keep **`docs/phase-8-cutover-runbook.md`** **Post-cutover release discipline** section accurate; extend **`AGENTS.md`** if needed. **No direct pushes to `main`** for routine work; **PRs** with **Cloudflare preview deployments** per branch; **merge to `main`** promotes **`www`**. Align **GitHub** branch protection and **Cloudflare Workers** (or **Pages**) project settings with this flow.
- [ ] **P9-26** **Post-cutover organic monitoring** (carried from Phase 8 **P8-26** **2026-04-14**): For **2–4 weeks** after **`www`** cutover (**2026-04-14** baseline), monitor **GSC** coverage (new **404**s, soft **404**s, crawl anomalies), organic traffic trend vs **P8-08** baseline (**`docs/phase-8-verification-record.md`** §GSC baseline snapshot), indexed page count, and **Core Web Vitals** field data. Check **Google cache** (URL Inspection **View Crawled Page**) on key pages. Log dated rows in **`docs/phase-8-verification-record.md`** §Organic monitoring log (this file stays the cutover-era record until **`docs/phase-9-verification-record.md`** exists). Cadence: daily the first week, then bi-weekly through week **4**. If organic traffic drops **>30%** for **3+ consecutive days** without external cause, investigate and escalate. Check this row when the window is complete and there is no unresolved SEO regression, or document resolution in **`docs/migration-exceptions.md`** / verification notes with owner.

## Performance optimization Agent Skill

- [x] **P9-10** Performance check recipe skill: when to run `npm run build`, `npm run lighthouse:all` (full / `LIGHTHOUSE_PATHS=…` subset / `LIGHTHOUSE_MIN=85` gate / `BASE_URL=…` origin), targeted drilldowns (`perf:impact`, `perf:blocking`, `perf:consent`), and how to read `docs/performance-improvement-plan.md` for pass vs needs-exception. Also contains **P9-40** stats bridge recipe (GSC / GA4 / sales queries via the sibling `andetag-stats` CLI — read-only, credentials live in that project's `.env`, this repo holds no extra secrets). Canonical: **`skills/performance-check/SKILL.md`**. Pointers: **`.claude/skills/performance-check`**, **`.cursor/rules/performance-check.mdc`**. Escalation: vendor drift with no PR-side culprit, headless-Chrome crashes, PSI requests (documented manual step), or stats-bridge gaps (do not fabricate numbers).
- [x] **P9-11** Published: canonical skill at `skills/performance-check/SKILL.md`; **Claude Code** pointer `.claude/skills/performance-check` (symlink); **Cursor** wrapper `.cursor/rules/performance-check.mdc`; listed in **`AGENTS.md`** doc table under `skills/README.md`.

## Content, SEO, and trust Agent Skills

- [x] **P9-12** Author a skill for **adding, editing, renaming, or removing pages** on **`site/`** (page shells, body registry, file-based routes, responsive images per **`docs/responsive-image-workflow.md`**, navigation/chrome, locale parity). Canonical file: **`skills/page/SKILL.md`**. Pointers: **`.claude/skills/page/SKILL.md`** (symlink) and **`.cursor/rules/page.mdc`**. Index row in **`skills/README.md`**; **`AGENTS.md`** layout + doc table updated.
- [x] **P9-13** SEO skill: canonicals, hreflang, metadata, structured data, OG/Twitter, robots, sitemap, keyword framing, internal linking, tone — bound to `docs/Andetag SEO Manual.md`, `docs/Tone of Voice.md`, `docs/url-migration-policy.md`, `docs/meta-texts-catalog.md`, `docs/migration-exceptions.md` (EX-0015/0016/0017/0019). Canonical: **`skills/seo/SKILL.md`**. Pointers: **`.claude/skills/seo`**, **`.cursor/rules/seo.mdc`**. OSS survey done: structural inspiration only from MIT `coreyhaines31/marketingskills/skills/seo-audit` and `JeffLi1993/seo-audit-skill` (§G "OSS survey notes" in the skill); nothing vendored because the substance is ANDETAG-specific (locked URL architecture, Museum + LocalBusiness graph per EX-0017, Berlin English → Stockholm English canonical per EX-0016, banned word list, Swedish-English-German tone asymmetry). Source-integrity rules forbid fabricated URLs, keywords, ratings, prices, or schema fields; approved sources are the docs + runtime modules (`page-shell-meta.json`, `page-shell-registry.ts`, `schema-org.ts`, `stockholm-*.ts`), plus `site-html/` as legacy-reference-only. Rich Results validation is manual (live deploy). Escalation: `CANONICAL_HOST` / `OG_SITE_NAME` / hreflang attribute changes; new keywords outside SEO Manual §2; new schema types not in §6; `noindex` on a previously indexable shell; Berlin pre-opening scope violations.
- [x] **P9-14** Testimonials skill: update TripAdvisor rating, review count, featured reviews, vendor URL. Canonical: **`skills/testimonials/SKILL.md`**. Pointers: **`.claude/skills/testimonials`**, **`.cursor/rules/testimonials.mdc`**. Anchors the single source at `site/src/lib/content/stockholm-reviews.ts` (`STOCKHOLM_TRIPADVISOR_URL`, `STOCKHOLM_RATING`, `STOCKHOLM_FEATURED_REVIEWS`, `stockholmTripadvisorRatingCommaDecimal()`); auto-propagating consumers are `schema-org.ts` (aggregateRating + review), `stockholm-testimonial-aggregate.ts` (sv/en/de strip), `StockholmHomeSv/En.astro` via `.map(r => …)`, and `Besokaromdomen{Sv,En}.astro` copy. **Drift hotspots** (require hand sync on quote edits): `StockholmSeoLandingSv.astro`, `StockholmSeoLandingEn.astro`, `StockholmHomeSharedBody.astro` each inline their own `testimonialItems` arrays. Quotes not translated across locales (TripAdvisor policy). `reviewCount` stays a JSON number per `docs/Andetag SEO Manual.md` §6 / EX-0017. Escalation: Berlin reviews, fabricated quotes, rating drop without explanation, TripAdvisor link removal (touches EX-0002/EX-0012).
- [x] **P9-16** FAQ skill: add, edit, remove, or reorder Stockholm FAQ entries. Canonical: **`skills/faq/SKILL.md`**. Pointers: **`.claude/skills/faq`**, **`.cursor/rules/faq.mdc`**. Covers `site/src/lib/content/stockholm-faq.ts`, shared `stockholm-what-is-andetag-faq-copy.ts`, marketing FAQ subsets, `schema-org.ts` FAQPage emission, and `sv + en` parity. Berlin FAQ escalation rule included.
- [x] **P9-17** Events skill: add, update, remove events on Stockholm pages (recurring Art Yoga + one-off home blocks). Canonical: **`skills/events/SKILL.md`**. Pointers: **`.claude/skills/events`**, **`.cursor/rules/events.mdc`**. Covers `stockholm-offers.ts` metadata, `computeArtYogaOccurrenceSeriesIso`, Understory booking CTAs, event hero responsive images, optional `Event` JSON-LD, `sv + en` parity. Escalation rules: single-occurrence skip (no mechanism), Berlin events, non-Understory booking domains.
- [x] **P9-19** Image suggestion skill: read `assets/images/photos.yaml`, suggest images for a page, wire through responsive pipeline. Canonical: **`skills/images/SKILL.md`**. Pointers: **`.claude/skills/images`**, **`.cursor/rules/images.mdc`**. Reads `assets/images/photos.yaml` (version 1; per-file `sv + en + de` alt + tags) for candidate selection; wires via `stockholm-body-responsive-images.ts` (body/aside/hero/testimonial), `assets.ts` (chrome heroes), and `stockholm-marketing-gallery.ts` (gallery tiles); derivatives per `docs/responsive-image-workflow.md` `SUFFIX` table. Outputs candidate table with rationale and catalog alt text before wiring. Escalation: no catalog match (no fabrication), Berlin page (no catalog entries), single-locale request, missing master, colour-vs-light claims.
- [x] **P9-18** Operational-facts skill: ticket prices, season pass, daytime window, opening hours, contact email, address, geo. Canonical: **`skills/operational-facts/SKILL.md`**. Pointers: **`.claude/skills/operational-facts`**, **`.cursor/rules/operational-facts.mdc`**. Flags **centralised** facts (`STOCKHOLM_TICKETS`, `STOCKHOLM_SEASON_PASS`, `STOCKHOLM_DAYTIME_WINDOW`, `STOCKHOLM_ART_YOGA_EVENT`, `STOCKHOLM_CURRENCY` in `stockholm-offers.ts`; `STOCKHOLM_ADDRESS`, `STOCKHOLM_GEO` in `schema-org.ts`) vs **not centralised** (opening hours across `OppettiderSv/En.astro` + `STOCKHOLM_OPENING_HOURS`; contact email across `booking-embed-contact.ts` + `schema-org.ts` + ~13 page bodies). Backlog items **M-0001** (hours) and **M-0002** (email) added to `docs/maintenance-backlog.md`. Escalation: holiday hours override (no date-bounded schema mechanism), new fact types (phone), currency-ambiguous price change, non-`andetag.museum` email without EX-0010 update, Berlin operational facts.
- [x] **P9-15** Published **P9-12** (`page`), **P9-13** (`seo`), **P9-14** (`testimonials`), **P9-16** (`faq`), **P9-17** (`events`), **P9-18** (`operational-facts`), **P9-19** (`images`), **P9-30** (`rollback`), **P9-31** (`site-integrity`) in the same pattern as **P9-11**: canonical at `skills/<name>/SKILL.md`, **Claude Code** symlink under `.claude/skills/<name>`, **Cursor** wrapper at `.cursor/rules/<name>.mdc`, row in `skills/README.md`, and **`AGENTS.md`** doc-table entry updated to list every landed skill (single index in `skills/README.md` is the source of truth).

## Integrity and recovery Agent Skills

- [x] **P9-30** Emergency content rollback skill: safe revert via `git revert` → PR → Cloudflare preview → merge, with locale-parity and JSON-LD spot-checks. Canonical: **`skills/rollback/SKILL.md`**. Pointers: **`.claude/skills/rollback`**, **`.cursor/rules/rollback.mdc`**. Covers full, partial, and range reverts; PR template; documentation trail (CHANGELOG `Removed`/`Fixed`, EX-NNNN dated note, optional M-NNNN forward-fix). Escalation: infrastructure rollback (cutover runbook), force-push or amend on `main`, cascading conflicts, approval-lineage conflicts, urgent takedowns that cannot wait for a preview cycle.
- [x] **P9-31** Site integrity skill: read-only audit across eight dimensions (internal link resolution, external link health, locale parity, canonical/hreflang, registry coherence, image-reference integrity, sitemap canonicalness, redirect-chain length). Canonical: **`skills/site-integrity/SKILL.md`**. Pointers: **`.claude/skills/site-integrity`**, **`.cursor/rules/site-integrity.mdc`**. Relies on existing Vitest coverage (`build-output-structure`, `page-shell-registry`, `url-matrix-parity`, `chrome-navigation-resolve`) for structural checks and adds grep-based drift detection between source modules and built `dist/`. External HEAD-checks are opt-in only. Outputs a structured report with `- none.` under clean dimensions. Escalation: build failures (fix via `page` / `rollback`), shipped SEO regressions, intentional multi-hop redirects.

## Production PR gate

- [ ] **P9-20** Define **mandatory** pre-merge checks for PRs that deploy to **`www`**: at minimum, the performance workflow from **P9-10** must run and pass unless waived (logged in **`docs/migration-exceptions.md`** or a Phase 9 verification note with owner).
- [ ] **P9-21** Optional: add or extend **CI** (GitHub Actions) for a performance budget or Lighthouse smoke on **`site/`** so the gate is not only manual.
- [ ] **P9-22** Record evidence model (where PRs or releases note “performance pass completed”).

## Verification record (when Phase 9 starts)

- [ ] Create **`docs/phase-9-verification-record.md`** when the program is no longer placeholder and sign-off is required (mirror Phase 8 record pattern). Until then, **P9-26** monitoring rows live under **`docs/phase-8-verification-record.md`** §Organic monitoring log.
