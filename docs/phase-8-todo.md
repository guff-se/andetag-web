# Phase 8 execution checklist, deployment and production cutover

Purpose: move the rebuilt site from **staging** (**`https://andetag-web.guff.workers.dev`**, Workers static assets + entry Worker) to **canonical production** (**`https://www.andetag.museum`**), with full verification before and after DNS or host cutover.

Normative phase summary: **`docs/grand-plan.md`** (Phase 8). Exit checks: **`docs/definition-of-done.md`** (Phase 8).

**Staging vs production:** **`www.andetag.museum`** serves this stack after cutover (**P8-11**, **2026-04-14**). **Pushes to `main`** redeploy **staging** (**`https://andetag-web.guff.workers.dev`**) and production **`www`** per deploy wiring. Pre-cutover checks were against **dev** and **staging**; remaining Phase 8 work is **post-cutover** on **`www`** (**P8-20** onward).

**After `www` cutover:** Use **pull requests** only for routine changes (no direct **`main`** pushes); each PR gets a **Cloudflare preview URL**; **merge** updates **`www`** (see **P8-25**).

## Moved here from earlier phases

| Source | What |
|--------|------|
| **`P5-06`** (production leg) | **Production enable** of entry Worker traffic on **`www`**, table **B** on **`https://www.andetag.museum`**, Gustaf sign-off (**`docs/phase-4-redirect-tests.md`**). Staging leg of **`P5-06`** stays as today: **`npm run verify:staging-entry`** on **`andetag-web.guff.workers.dev`**. |
| **`docs/grand-plan.md`** **Entry routing and URL expansion schedule** | Row **Production enable** of the entry Worker (DNS or custom domain binding so **`www`** hits this deployment). |
| **Grand-plan dependency rule** | Re-run table **B** on **`www`** after cutover; log evidence. |
| **`docs/cookieconsent-migration-plan.md`** | **Closed** record of the in-repo CookieConsent migration (exit criteria are only what that doc’s closure table lists). **GTM** and **live tag verification** belong to **this phase** (**P8-07**, **P8-13**, **P8-22**), not to remaining work inside the migration plan. |

**Phase 7 (closed):** in-repo scripts, **CookieConsent** embed, **Consent Mode** defaults, sitemap, schema, favicon, sharing metadata, and staging-documented validation (**`docs/phase-7-verification-record.md`**). **GTM** container migration (**`docs/gtm-consent-migration-runbook.md`**) is **Phase 8 · P8-07**, timed **just before** cutover. Phase 8 re-runs the **live** checks that depend on **`www`** (and closes **`P5-06`** production).

## Pre-cutover: dev and staging

- [x] **P8-05 Phase 7 gate (or waiver):** Phase 7 checklist (**`docs/phase-7-todo.md`**) is **closed** with **Gustaf** sign-off **2026-04-09** (**`docs/phase-7-verification-record.md`** §P7-16). Deferred work (**GTM** container migration) is **P8-07**, logged; no waiver required. **Promotion to `www`** still requires **P8-06** and the remaining Phase 8 pre-cutover rows.
- [x] **P8-01 Local dev QA:** On **`npm run dev`** (or agreed local preview after **`npm run build`**), exercise **routing** (entry paths, locale switches, **`301`** chains from **`docs/url-matrix.csv`** / **`site/public/_redirects`**), **scripts** (GTM, CookieConsent, embeds per Phase 7), **cookies** (**`andetag_entry`**, consent categories), **forms** (Brevo waitlist, booking embeds), and **core conversion paths**. Fix regressions or log **`docs/migration-exceptions.md`** with Gustaf approval. **Pass 2026-04-12:** Vitest 28 files / 110 tests; Astro build 63 pages; staging canonical spot-check 200 OK; 404 for unknown paths.
- [x] **P8-02 Staging parity:** Confirm staging (**`https://andetag-web.guff.workers.dev`**) matches expected behavior after merge to **`main`** (auto-deploy). Re-run **`npm run verify:staging-entry`** after any Worker, **`_redirects`**, or entry-policy change; append **`docs/phase-4-redirect-tests.md`** if you use manual **`curl`** instead. **Pass 2026-04-12:** `npm run verify:staging-entry` 11/11 entry checks pass; Table A 14/14 static redirect checks pass via `curl` against staging.
- [x] **P8-03 Old-site URL coverage:** Every **must-keep** URL from the legacy live site (source: **`docs/url-matrix.csv`**, sitemap or inventory from **`spider.py`**, and agreed aliases) **resolves** on dev and staging as **200** (or agreed **301** to canonical), per **`docs/url-migration-policy.md`**. Additionally, cross-reference the **top backlinked URLs** from **Google Search Console** (or available third-party data) against the matrix to confirm no high-value inbound link targets are missing. Gaps are **not** silent: record in **`docs/migration-exceptions.md`** and get **Gustaf confirmation** before cutover. **Pass 2026-04-12:** 61/61 canonical `keep` URLs verified in `dist/`; 23/24 redirect rules in `_redirects` (remaining `/` is Worker-owned); 0 legacy sitemap gaps; **backlink cross-reference completed via P8-08 GSC export:** 49 GSC pages checked, 5 redirect gaps found and fixed (see verification record §GSC baseline snapshot); 28/29 redirect rules now in `_redirects` plus Worker-owned `/`.
- [x] **P8-08 Search Console baseline:** Export **Google Search Console** data before cutover: top pages by clicks and impressions, top queries, indexed page count, crawl stats, and coverage report. Verify the GSC property covers **`www.andetag.museum`** (create or re-verify if needed). This baseline is the reference for detecting SEO regression after cutover. Save a snapshot in **`docs/phase-8-verification-record.md`**. **Pass 2026-04-12:** GSC property `https://www.andetag.museum` verified. 90-day export (2026-01-11 to 2026-04-10): 7,176 clicks, 37,223 impressions, 43 indexed pages, 93 crawl requests/day avg. Last 28 days: 72.6 clicks/day, 19.3% CTR. Coverage: 8 not-found errors, 2 redirect errors. Full baseline in `docs/phase-8-verification-record.md` §GSC baseline snapshot.
- [x] **P8-09 Internal link audit:** Crawl staging (or local build output) and verify that **internal `<a href>` links** point to canonical URLs, not redirect-source URLs. Internal links through **`301`** redirects waste crawl budget, dilute link equity, and add latency. Check navigation, footer, page bodies, and programmatically generated links (registry, sitemap). Fix any that redirect; log approved exceptions. **Pass 2026-04-12:** 63 HTML files scanned, 3,869 internal `<a href>` links; 0 point to redirect-source paths. Header, footer, and body links all use canonical paths.
- [x] **P8-04 Exception sign-off:** No undocumented deviations: all intentional differences vs the old live site are listed, approved, and owned. **EX-0017** (schema.org changes) must be **approved** before cutover (not deferred to post-cutover re-check). **Approved 2026-04-12** (evolving with **EX-0017**): Stockholm venue **`Museum`** + paired **`LocalBusiness`** (Google review-snippet parent types), **`aggregateRating`**, **`review`**, **6 `Offer`** nodes (**`stockholm-offers.ts`**), **four dated Art Yoga `Event`** nodes. Legacy standalone **`ArtGallery`** dropped; legacy **`TouristAttraction`** dual type not used. All exceptions approved.
- [x] **P8-10 Cutover runbook:** Written steps for DNS, Cloudflare **custom domain** / **Workers** (or Pages) binding, TLS, and rollback (revert DNS or detach domain). Align with **`docs/url-migration-policy.md`** and hosting account reality. Include: **DNS TTL reduction** (lower to 60-300s at least **24-48 hours** before cutover so propagation is fast on the day); whether **Cloudflare edge cache** needs a purge at cutover to avoid serving stale WordPress responses; and the **post-cutover release discipline** (**P8-25**). Draft this **early** (alongside **P8-01**-**P8-04**), not at the last minute. **Drafted 2026-04-12:** **`docs/phase-8-cutover-runbook.md`**. Covers Workers custom domain (preferred), Workers route fallback, rollback, cache purge, and P8-25 PR discipline.
- [x] **P8-06 Locale copy sign-off (staging, blocks production):** Gustaf explicitly approves **final user-visible text** for **`sv`**, **`en`**, and **`de`** on **staging** (use an agreed checklist: hubs, one inner page per destination per locale, legal or privacy, and nav or chrome strings if needed). Optional: external reviewer for **`de`**; Gustaf still signs **`de`**. Record dates and scope in **`docs/phase-8-verification-record.md`**. **Do not** treat staging as **ready for `www`** until **P8-06** is complete. **Signed off 2026-04-12 (Gustaf):** Live-vs-staging copy audit confirmed zero unintentional drift. All body text verbatim across sv/en/de. Every visible difference (static testimonials EX-0002, contact email EX-0010, schema EX-0017, consent EX-0018, spelling EX-0019) is logged and approved. Important copy improvements deferred to post-migration to minimize SEO change surface at cutover.
- [x] **P8-07 GTM + CookieConsent container migration (just before production cutover):** Execute **`docs/gtm-consent-migration-runbook.md`** end to end in **`GTM-KXJGBL5W`** (replace legacy **`cmplz_*`** coupling with consent-aware trigger strategy for static pages, keep tag-level Additional Consent Checks, review linker domains). **Publish** and **GTM Preview** on **`https://andetag-web.guff.workers.dev`**: verify consent flow, minimum **page_view**, and conversion path per runbook §4. **Intended timing:** immediately before **`P8-11`** (after **P8-06** and other pre-cutover gates), so the container is not left half-migrated during a long staging window. **EX-0018** and **`docs/kpi-measurement-map.md`** § Staged rollout apply. **Live** domain focus and re-verify: **P8-13**, **P8-22**. **Pass 2026-04-14:** Parts A–C on staging then publish; **Part D** on **`www`** after static-only (remove **`cmplz_*`** triggers on GA4 / Google Ads / Meta **All pages** tags, publish); evidence in **`docs/phase-8-verification-record.md`** §Cutover.

## Cutover

- [x] **P8-11 Execute cutover:** Point **`www.andetag.museum`** (and apex policy if applicable) at this deployment so the **entry Worker** and static assets serve production traffic. **Pass 2026-04-14:** **`www`** on Worker **`andetag-web`**; apex redirect to **`www`** (see Gustaf DNS / Redirect Rules work).
- [x] **P8-12 Immediate smoke:** Right after cutover, spot-check **`/`**, **`/en/`**, **`/sv/stockholm/`**, sample inner pages, and one **Berlin** path; confirm **HTTPS** and no accidental **mixed content** for first-party assets. **Pass 2026-04-14:** Curl path checks and **`robots.txt`** / **`sitemap-index.xml`** OK; Chrome console without mixed-content failures; **`andetag_entry`** cookie confirmed on **`www`**; CookieConsent (CMP) exercised.
- [x] **P8-13 Consent or tag primary domain switch:** After **`P8-07`** and **`P8-11`**, in GTM admin, switch primary production domain references from staging to **`www.andetag.museum`**, publish, then verify banner load and consent-gated tag behavior on live **`www`**. Reconfirm **GA4 / Ads / Meta** fire after consent (not only on legacy **`cmplz_*`** triggers); see **`docs/kpi-measurement-map.md`** § GTM migration checklist. **Pass 2026-04-14:** Live GTM Preview / Tag Assistant aligned with runbook Part D; consent-gated tags verified with CookieConsent on **`www`**.

## Post-cutover: live **`www`**

- [ ] **P8-23 SEO and sharing on live URLs (same day):** Verify **`robots.txt`** at **`https://www.andetag.museum/robots.txt`** returns the correct rules (allows **Googlebot**, references the production sitemap URL, does **not** carry over any staging **`Disallow`** rules). Verify **sitemap** at the production origin resolves and lists only canonical indexable URLs. **Submit** the new sitemap in **Google Search Console**. Request indexing of the **5-10 highest-value pages** via URL Inspection. Spot-check **Open Graph** / card validators and **JSON-LD** on representative **`www`** URLs (**`docs/phase-7-todo.md`** cross-check). Run **[Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)** (and **Scrape Again** if needed) on key **`https://www.andetag.museum/...`** URLs: confirm **`og:image`** resolves with **`image/jpeg`** (or another image **`Content-Type`**), not HTML (**invalid content type** usually means **`404`** or wrong host before cutover). Default share image path is documented in **`site/src/lib/chrome/assets.ts`** (**`HERO_SV_ASSETS.poster`**); absolute URL uses **`https://www.andetag.museum`** via **`site/src/lib/chrome/seo.ts`**. **Partial 2026-04-14:** **`robots.txt`** / **`sitemap-index.xml`** / **`og:image`** curl spot-checks pass; **GSC** submit, URL Inspection batch, **Facebook** debugger still **operator** (see **`docs/phase-8-verification-record.md`**).
- [x] **P8-20 Entry routing on production:** Run table **B** against **`https://www.andetag.museum`** (for example **`STAGING_BASE=https://www.andetag.museum npm run verify:staging-entry`** from **`site/`**, or manual **`curl`** per **`docs/phase-4-redirect-tests.md`**). **Log** date and result; this closes **`P5-06`** production. **Pass 2026-04-14:** **`STAGING_BASE=https://www.andetag.museum npm run verify:staging-entry`** from **`site/`** — all **11** checks (**E1**–**E11**); logged in **`docs/phase-4-redirect-tests.md`** and **`docs/phase-8-verification-record.md`**.
- [x] **P8-21 Redirect and matrix regression:** Execute or extend **`docs/phase-4-redirect-tests.md`** table **A** (and any CI link checks) against **`www`** where they are host-sensitive. **Pass 2026-04-14:** **`curl -sI`** against **`https://www.andetag.museum`** for table **A** cases (including **`/en/about-andetag/`**, **`/om-andetag/`**, **`/de/ueber-andetag/`**): **14/14** **301** with expected **`Location`** paths (query preserved on **`/de/?utm_source=test`**); logged in **`docs/phase-4-redirect-tests.md`**.
- [ ] **P8-22 Live feature pass:** Repeat **P8-01** scope on **`www`**: consent, tags, embeds, cookies, critical forms, and locale switching. Include a booking test that **`on_receipt`** (or equivalent) still reaches conversion tags if you rely on legacy Understory **`dataLayer`** wiring (**`docs/kpi-measurement-map.md`**). Run a **Lighthouse** or Core Web Vitals spot-check on representative conversion pages to establish a **production performance baseline** (targets per **`docs/definition-of-done.md`** shared quality gates; baseline feeds **`docs/performance-improvement-plan.md`** and Phase 9). **Partial 2026-04-14:** **Lighthouse** mobile (performance only) on **four** URLs in **`docs/phase-8-verification-record.md`** §Production Lighthouse baseline. **Still open:** full **P8-01** sweep, **booking** / **`dataLayer`** receipt path.
- [ ] **P8-25 Post-cutover release discipline:** Document in the **P8-10** runbook (and **`AGENTS.md`** if needed): **no direct pushes to `main`** for routine work; **PRs** with **Cloudflare preview deployments** per branch; **merge to `main`** promotes **`www`**. Align branch protection and Workers or Pages project settings with this flow.
- [ ] **P8-26 Post-cutover monitoring (2-4 weeks):** Monitor organic health daily for the first week, then bi-weekly through week 4: **GSC** coverage report (new **404**s, soft **404**s, crawl anomalies), organic traffic trend vs baseline (**P8-08**), indexed page count, and **Core Web Vitals** field data. Check **Google cache** (URL Inspection "View Crawled Page") on key pages to confirm Google sees the new site. Log observations in **`docs/phase-8-verification-record.md`**. If organic traffic drops **>30%** for **3+ consecutive days** without external cause, investigate and escalate before proceeding to closure.
- [ ] **P8-24 Closure:** Update **`docs/grand-plan.md`** Phase 8 status, **`docs/phase-8-verification-record.md`** with evidence and Gustaf sign-off, and **`CHANGELOG.md`**. **Gate:** do not close until **P8-26** monitoring confirms no SEO regression requiring intervention. Confirm **`docs/phase-9-todo.md`** exists and is ready to receive maintenance-program work. Hand off ongoing organic monitoring to Phase 9.

## References

- **`docs/grand-plan.md`** (Phase 8, entry schedule, rollout order)
- **`docs/url-migration-policy.md`**, **`docs/phase-4-routing-reopen.md`**, **`docs/phase-4-redirect-tests.md`**
- **`docs/phase-7-todo.md`** (Phase 7 **closed**; **GTM** runbook: **P8-07**)
- **`docs/kpi-measurement-map.md`**, **`docs/tracking-and-consent-requirements.md`** (GTM + consent cutover, **`cmplz_*`** trigger replacement, Understory **`dataLayer`**)
- **`docs/cookieconsent-migration-plan.md`** (closed in-repo CMP migration; optional background for **P8-07** / **P8-22**)
- **`docs/migration-exceptions.md`**
- **`AGENTS.md`** (Routing and entry URLs)
